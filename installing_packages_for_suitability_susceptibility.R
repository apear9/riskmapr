### Run this command to install the R packages needed to run 'shiny_app_suitability.R' and 'shiny_app_susceptibility.R'

install.packages(
  c("rgdal", "raster", "rgeos", "sp", "zip", "shiny", "dplyr", "truncnorm", "stringr", "visNetwork")
)